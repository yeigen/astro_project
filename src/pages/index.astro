---
import Layout from "../layouts/Layout.astro";
import Cards from "../components/Cards.astro";
import About from "../components/About.astro";
import { about } from "../data/about";
import  Carrusel from "../components/Carrusel.astro";

const pageTitle = "Portfolio — Gabriel Martínez";
const pageDescription = "Desarrollador. Proyectos, habilidades y formas de contacto.";
---

<Layout title={pageTitle} description={pageDescription}>
<div class="flex flex-col lg:flex-row items-center justify-center gap-8 md:gap-12 lg:gap-20 px-4 py-12">
  <div class="flex flex-col items-center justify-items-center">
    <img
      class="md:block w-32 h-32 lg:w-48 lg:h-48 border-2 border-b-blue-100 rounded-full object-cover transition-transform duration-300 hover:scale-105"
      src="/yeigen.png"
      alt="Foto de Gabriel Martínez"
      loading="lazy"
    />

    <div class="mt-4 flex flex-wrap justify-center items-center gap-3">
      {about.url_images_social.map((icon) => (
        <a href={icon.href} target="_blank" rel="noopener noreferrer" aria-label={`${icon.title} de Gabriel`}>
          <img
            class="size-10 p-1 bg-white rounded-full shadow hover:scale-110 transition-transform object-contain shrink-0"
            src={icon.src}
            alt={`Logo de ${icon.title}`}
            loading="lazy"
          />
        </a>
      ))}
    </div>
  </div>

    <div class="relative flex flex-col items-center gap-4 bg-black/30 backdrop-blur-lg rounded-lg p-8 shadow-lg shadow-amber-200/50 w-full max-w-lg">
      <h1 class="text-3xl md:text-4xl font-bold text-center leading-tight">Yeigen (Gabriel Martínez)</h1>
      <p class="text-center text-gray-200">
        Me gusta utilizar python para hacer proyectos, lo que más me gusta es el backend, pero estoy
        aprendiendo un poco de frontend ya que también me parece importante por ahora solo tengo 1 proyecto que es 
        <a href="https://abodiapp.com/" target="_blank" rel="noopener noreferrer" class="text-amber-200 font-semibold underline hover:no-underline"> Abodi</a>.
      </p>
      <a href="#contacto" class="text-xl font-bold mt-2 text-amber-200 hover:text-amber-300">Contacto</a>
    </div>
    
    <img
      src="/guts_blade.png"
      alt="Ilustración temática"
      class="lg:block w-48 h-48 object-contain transition-transform duration-300 hover:scale-105"
      loading="lazy"
    />
  </div>
  <Cards />

  <About />

  <Carrusel />

</Layout>
