---
const { id, title, className = "" } = Astro.props;
---
<div id={id} class={`w-full max-w-6xl mx-auto animate-in fade-in slide-in-from-bottom-1 duration-300 ease-out ${className}`}>
  {title && <h2 class="text-3xl md:text-4xl font-bold text-amber-200 mb-6 text-center">{title}</h2>}
  <slot />
</div>
